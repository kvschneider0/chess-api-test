name: Deploy to AWS CloudFormation - main

on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - name: serverless deploy
        uses: serverless/github-action@v3.1
        with:
          args: deploy -s main
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_ID }}
# name: Deploy to AWS CloudFormation - main

# on: 
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to AWS Cloud Formation
#         uses: aws-actions/aws-cloudformation-github-deploy@v1
#         with:
#           name: node-api-main
#           template: serverless.yml
#           parameter-overrides: "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }},AWS_SECRET_ACCES_KEY_ID=${{ secrets.AWS_SECRET_ACCES_KEY_ID }},stage=main"